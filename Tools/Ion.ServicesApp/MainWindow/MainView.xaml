<Window x:Class="Ion.ServicesApp.MainWindow.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ion.ServicesApp.MainWindow"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        Title="MainView" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Start" Command="{Binding StartCommand}" Visibility="{Binding CanStart, Converter={StaticResource BoolToVis}}" />
            <MenuItem Header="Stop" Command="{Binding StopCommand}" Visibility="{Binding CanStop, Converter={StaticResource BoolToVis}}" />
            <MenuItem Header="Explore" Command="{Binding ExploreCommand}" />
            <MenuItem Header="Delete" />
        </ContextMenu>
        <Style x:Key="DefaultRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="ContextMenu" Value="{StaticResource RowMenu}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Signed}" Value="False">
                    <Setter Property="Background" Value="#ffcf70" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Exists}" Value="False">
                    <Setter Property="Background" Value="Gray" />
                    <Setter Property="Foreground" Value="WhiteSmoke" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Services}" SelectedValue="{Binding Selected}" AutoGenerateColumns="False" RowStyle="{StaticResource DefaultRowStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName, Mode=OneWay}" />
                <DataGridTextColumn Header="Service" Binding="{Binding ServiceName, Mode=OneWay}" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type, Mode=OneWay}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status, Mode=OneWay}" />
                <DataGridTextColumn Header="Created" Binding="{Binding Created, Mode=OneWay}" />
                <DataGridTextColumn Header="Path" Binding="{Binding Path, Mode=OneWay}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>